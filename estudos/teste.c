#include <stdio.h>

    int main() {
        int numero1;
        int numero2;

        numero1 = 10;
        numero2 = 10;

        if (numero1 >= numero2)
        {
         /* bloco vai ser executado quando a condição for verdadeira*/
           printf("O número 1 é maior que o número 2\n");
        } 
        else
        {
            /* bloblo */
            printf("O número 1 é menor que o número 2\n");
        }
        
   
}