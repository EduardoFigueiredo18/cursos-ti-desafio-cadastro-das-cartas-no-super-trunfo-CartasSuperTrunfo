#include <stdio.h>
int main()
{

    int idade = 16;

    if (idade >= 18)
    {
        printf("Você é maior de idade, já pode votar!\n"); // se for verdadeiro
    }
    else
    {
        printf("Você ainda é menor de idade, não poder votar!\n"); // se for falso
    }
}