#include <stdio.h>
int main()
{
    int nota = 50;
    if (nota >= 60)
    {
        printf("Você passou!\n"); // se for verdadeiro
    }
    else
    {
        printf("Você foi reprovado!\n"); // se for falso
    }
}